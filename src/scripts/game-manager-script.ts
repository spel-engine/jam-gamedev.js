import { AbstractScript, Interface } from '@spel/core';
  
export class GameManagerScript extends AbstractScript {
    private currentLevel: number = 0;

    private features = {
        doubleJump: false,
    }
    
    override start(): void {
    
    }
    
    override update(): void {
    
    }


}
